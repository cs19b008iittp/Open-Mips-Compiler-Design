void main()
{
	string str;
	print("Enter the string");
	str = scan();

	string rev = "";
	for( int i = len(str) - 1 ; i >= 0 ; i-- )
	{
		rev += str[i];
	}

	print("Reverse of ", str, " is ", rev);
}
